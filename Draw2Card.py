import random

def draw2Card(cardColor,cardCode):
    super(40,cardColor,cardCode)
    
    def str(lineNumber):
        if (lineNumber < 0):
            return super.str(lineNumber*(-1))
    cardColorCode = color.str(super.cardColor())
    switch(lineNumber)
    for case in one:
        for case in seven:
            return cardColorCode + "●~~~~~~~●" + color.str("reset")
    for case in two:
        return cardColorCode + "|+2    |"+ color,str("reset")
    for case in three:
        for case in five:
            return cardColorCode + "|        |" + color.str("reset")
        for case in four:
            return cardColorCode + "| Draw2 |"+ color.str("reset")
    for case in six:
        return cardColorCode + "|        +2|"+color.str("reset")
    for case in eight:
        return color.str("Red")+ "code: "+ super.cardCode()+ color.str("reset")
    return null

