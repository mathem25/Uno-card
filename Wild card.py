import random

def wildCard(cardCode):
    super (40,"Blue",cardCode)
    def str(lineNumber):
        if (lineNumber < 0):
            return super.str(lineNumber*(-1))
    switch(lineNumber)
    for case in one:
        for case in seven:
            return color.str("Red") + "●~~~~~~~●" + color.str("reset")
    for case in two:
        return color.str("Red") + "|☯          |"+ color.str("reset")
    for case in three:
        for case in five:
            return color.str("Red") + "|        |" + color.str("reset")
        for case in four:
            return color.str("Red") + "|" + color.str("Blue")+"R"+color.str("Green")+"o"+color.str("Yellow")+"n"+color.str("Purple")+"f"+color.str("Red")+"|"+ color.str("reset")
    for case in six:
        return color.str("Red") + "|         ☯|"+color.str("reset")
    for case in eight:
        return color.str("Red")+ "code: "+ super.cardCode()+ color.str("reset")
    return null

